# Copy to .env and fill in. The app also accepts a key typed into the sidebar,
# which takes precedence over this file.
OPENAI_API_KEY=sk-your-key-here

# Optional overrides — any Config field can be set with a DOCCHAT_ prefix.
# DOCCHAT_LOG_LEVEL=DEBUG
# DOCCHAT_RETRIEVAL_MODE=hybrid_rerank
# DOCCHAT_RETRIEVAL_K=4
# DOCCHAT_HYBRID_ALPHA=0.5
